# STM32 测量组项目统一开发规则

## 1. 适用范围与开发环境

- 本规则适用于 `D:\CubeIDE\h7_pre` 下现有及未来的实际 STM32 工程。
- 不得把 `.metadata`、`STM32CubeIDE_1.19.0`、`STM32CubeIDE_2.1.1` 或其他 IDE 安装目录当作项目进行编辑或初始化 Git。
- 开发软件为 STM32CubeIDE 1.19.0。
- 目标 MCU 为 STM32H750VBT6。
- 项目用于大学生电子设计竞赛测量组；实现时优先考虑实时性、可靠性、可调试性和资源占用。
- 不得猜测硬件连接、引脚、时钟或外设配置。缺少关键信息时先向用户确认。

## 2. 用户代码目录与文件组织

- 所有用户编写的 `.c` 和 `.h` 文件必须放在 `<项目根目录>\Core\User\`。
- `Core\User\system.h` 是用户代码唯一的统一头文件入口：
  - 在其中统一包含全部用户模块头文件；
  - 在其中集中声明需要跨文件访问的 `extern` 变量；
  - 使用头文件保护，并正确处理 HAL 类型和模块之间的依赖。
- `main.c` 和其他用户 `.c` 文件只能自行添加 `#include "system.h"`。
- 不得擅自删除 STM32CubeIDE/CubeMX 自动生成且维持工程结构所需的 include。
- 不得把新用户模块散放到 `Core\Src`、`Core\Inc` 或其他目录。

## 3. 初始化与主循环

- 所有用户自定义初始化逻辑必须写在 `Core\User\system.c` 的 `void system_init(void)` 中。
- `main.c` 的用户初始化区只能调用：

```c
system_init();
```

- CubeMX 自动生成的 HAL、系统时钟和 `MX_*` 初始化代码保持在生成位置，不迁入用户文件。
- `main.c` 的 `while (1)` 用户代码区只能调用功能实现函数，不得直接堆放业务逻辑、算法细节或外设处理过程。

## 4. 中断与标志变量

- 每个中断回调函数只能给一个与该中断目的对应的 `xxx_flag` 标志变量赋值。
- 中断共享标志通常使用 `volatile` 修饰，并在 `system.h` 中进行 `extern` 声明。
- 中断回调内禁止执行延时、循环、打印、复杂计算、协议解析或耗时外设操作。
- 实际业务处理及标志清除必须放入由主循环调用的功能实现函数。
- 如 HAL 或硬件强制要求额外清除中断状态，先向用户说明原因和处理位置；不得借此把业务逻辑放入中断。

## 5. STM32CubeIDE 与 MCU 初始化配置

- 如果功能需要调整 `.ioc`、Pinout、Clock Configuration、GPIO、DMA、NVIC 或外设参数，不得直接修改相关配置或 CubeMX 生成的初始化代码。
- 必须先向用户说明：
  1. STM32CubeIDE 1.19.0 中的具体配置入口；
  2. 需要打开的页面和选项；
  3. 每个需要修改的参数及建议值；
  4. 对 GPIO、DMA、中断、时钟和其他外设的影响；
  5. 重新生成代码的方法；
  6. 防止用户代码被覆盖的注意事项。
- 等待用户完成配置或明确授权后，再继续编写依赖该配置的代码。

## 6. 命名规范

- 所有用户自定义函数名和变量名必须使用小写下划线命名法，例如 `measure_voltage()`、`adc_ready_flag`。
- `system_init()` 使用小写下划线命名法。
- STM32CubeIDE、CubeMX、CMSIS 和 HAL 自动生成或规定的名称保持原样，不强制重命名。

## 7. 注释与模块说明

- 先完成代码实现和必要验证，再统一补充注释。
- 所有新增或修改的用户代码必须在交付前完成中文注释。
- 每个用户函数都要用中文说明功能、参数、返回值和重要副作用。
- 每个重要变量都要在声明处用中文说明用途；中断共享变量还要说明并发用途。
- 每个用户模块的 `.c` 和 `.h` 文件开头必须写明：
  - 模块用途；
  - GPIO 引脚映射；
  - 依赖的外设和 CubeIDE 配置；
  - 初始化方法；
  - 调用方法。
- 模块没有直接使用 GPIO 时，明确写明“无直接 GPIO 引脚”。

## 8. Git 版本管理

- 每个实际 STM32 工程独立使用 Git；不得在 `D:\CubeIDE` 根目录或 IDE 安装目录初始化总仓库。
- 如果实际工程还没有 Git，在第一次修改代码前初始化本地仓库。
- 开始修改前必须检查 Git 状态，保留用户已有改动，不得覆盖、回滚或提交无关改动。
- 配置适合 STM32CubeIDE 的 `.gitignore`，排除构建产物、IDE 工作区元数据和本机临时文件，但保留源码、`.ioc` 和必要工程配置。
- 使用内容清晰的本地提交保证版本可回溯。
- 只有用户明确指定的比赛正式工程才能关联：
  - 远程仓库：`https://github.com/zzzmomo-code/electric-sai-stm32-measurement.git`
  - 目标分支：`ljz`
- 学习和测试工程只进行本地 Git 管理，除非用户另行指定远程仓库。
- 完成代码、验证和 README 后先向用户汇报。只有获得用户明确许可，才能 push 到远程仓库。

## 9. README 要求

比赛正式工程的 README 至少写明：

- 项目用途及实现的测量功能；
- MCU 型号和 STM32CubeIDE 版本；
- 硬件连接及 GPIO 引脚映射；
- CubeIDE/CubeMX 配置方法；
- 工程目录结构；
- 编译、烧录和运行方法；
- 各用户模块的初始化及调用方法；
- 已知限制和验证方法。

## 10. 工作完成条件

- 检查所有用户文件是否位于 `Core\User\`。
- 检查 `main.c` 用户初始化区是否只有 `system_init();`。
- 检查 `while (1)` 用户区是否只有功能实现函数调用。
- 检查中断回调是否只修改一个目的明确的标志变量。
- 检查用户自定义标识符是否使用小写下划线命名法。
- 检查中文注释、模块说明和 GPIO 映射是否完整。
- 运行当前条件允许的编译、静态检查或测试，并如实报告结果及未验证部分。
- 完成 README。
- 完成上述事项后等待用户确认；未经明确许可不得 push。

## 11. 启用、关闭和修改本规则

- 启用：保留本文件名 `D:\CubeIDE\AGENTS.md`，并从 `D:\CubeIDE` 或其项目目录启动新的 Codex 任务。
- 临时整体关闭：将本文件改名为 `AGENTS.md.disabled`，然后启动新的 Codex 任务。
- 临时关闭单条：用户在当前任务中明确说明暂停的规则编号和范围；用户当前直接指令优先。
- 永久增加、删除或修改：直接编辑本文件的对应编号条款。
- 单项目补充或覆盖：在具体项目根目录创建新的 `AGENTS.md`，明确写出新增内容以及被替代的总规则编号。

## 12. H750/ADS8688 基线与团队协作

### 12.1 当前迁移基线

- 本节优先于第 1、8、10、11 节中与工程路径、远程分支和 push 有关的旧表述；本地实际工程以本仓库根目录为准。
- 当前有效代码基线为 STM32H750VBT6 + ADS8688，来源是公共仓库的 `ljz` 分支。
- 旧 STM32F407 + AD7606 工程属于历史版本；不得把其源码、`.ioc`、构建产物或提交历史合并回 H750/ADS8688 主线。
- `ljz` 与旧 `main` 没有共同 Git 历史。迁移到新的 `main` 时，不得使用普通 merge 或 `--allow-unrelated-histories` 把两套工程混在一起。
- 执行 `main` 替换前，维护者必须先保留旧 F407/AD7606 的只读历史分支或标签，例如 `legacy/f407-ad7606-YYYYMMDD`；替换完成后，H750/ADS8688 的 `main` 才是唯一日常同步来源。
- ADS8688 的实际硬件连接、SPI2/DMA/NVIC 参数以 `h7_pre.ioc` 和 README 为准。任何实板吞吐、精度或稳定性结论必须标注“待硬件验证”。
- STM32H750 使用 DMA 时，DMA 缓冲区必须满足 32 字节对齐，并在 CPU 与 DMA 交换缓冲区前按实际内存区域完成 D-Cache 维护；不得因旧 F4 无缓存的经验省略此检查。

### 12.2 三仓协作目标结构

- 公共仓库是唯一集成源，建议放入团队 GitHub Organization；两名成员各自保有一个个人 fork，这三者共同构成协作结构。
- 每位成员的本地仓库配置两个远程：`origin` 指向自己的个人仓库，`upstream` 指向公共仓库。
- 当前仓库仍由个人账号持有且尚未建立 fork 结构。在组织公共仓库和两个个人 fork 建好前，公共 `origin` 仅可用于迁移基线与经双方确认的功能分支；禁止直接覆盖 `main`。

### 12.3 日常开发流程

1. 开始工作前获取公共最新代码：迁移期同步 `upstream/ljz`；`main` 切换完成后同步 `upstream/main`。未完成的功能分支不直接作为同步基线。
2. 从已同步的基线创建个人功能分支，名称使用 `成员名/功能名`，例如 `momo/uart-diagnostics` 或 `ljz/ads8688-range`。
3. 修改后依次执行本地提交、项目测试、工程编译；未通过测试或编译不得推送为待合并版本。
4. 测试通过后仅推送个人功能分支到 `origin`，再向公共仓库对应基线创建 PR；不得直接 push 公共 `main`。
5. 合并前将功能分支更新到公共最新基线，解决冲突后重新测试和编译；仅在个人功能分支上可使用 `--force-with-lease` 更新重写后的提交，绝不对公共 `main` 使用强推。
6. 公共 `main` 应启用分支保护：必须经 PR 合并、禁止强推与删除；两人协作时由另一位成员进行代码检查后合并。

### 12.4 本地命令约定

- `git add` 加 `git commit` 表示提交到本地历史；`git push` 才会发送到远程。
- 同步公共基线时优先使用 `git pull --ff-only`，避免自动生成无意义的合并提交。
- 发现远程分支与当前分支没有共同历史时，立即停止 merge，先确认它是否属于同一工程和同一硬件平台。
